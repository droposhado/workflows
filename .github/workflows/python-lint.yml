name: Python lint
on:
  workflow_call:
    inputs:
      python_version:
        default: '3.10.2'
        required: false
        type: string

jobs:

  linter:

    runs-on: ubuntu-latest

    steps:

      - name: Checkout repo
        uses: actions/checkout@v2

      - name: Linter
        run: echo 0
